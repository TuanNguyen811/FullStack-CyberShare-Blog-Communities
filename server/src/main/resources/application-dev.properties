# Development Profile Configuration
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/cybershare?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1q2w3e4R5613!
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# JWT
app.jwt.secret=dev-secret-key-change-in-production-a2cce5f4-25f9-454f-885d-1d6ce34621dc-super-long-secret
app.jwt.access-token-expiration-ms=3600000
app.jwt.refresh-token-expiration-ms=604800000

# CORS - Allow frontend dev server
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:3000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
app.upload.dir=uploads/avatars

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging
logging.level.com.server.server=DEBUG
logging.level.org.springframework.security=DEBUG
